[pytest]
testpaths = tests
markers =
    unit: does not require LLM credentials or external services
    online: requires configured LLM credentials or an external service
    integration: requires an installed optional runtime dependency
